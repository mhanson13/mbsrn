apiVersion: apps/v1
kind: Deployment
metadata:
  name: mbsrn-www
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mbsrn-www
  template:
    metadata:
      labels:
        app: mbsrn-www
    spec:
      containers:
        - name: www
          image: us-central1-docker.pkg.dev/PROJECT_ID/mbsrn/www:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
