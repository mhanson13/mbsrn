apiVersion: v1
kind: ConfigMap
metadata:
  name: work-boots-config
data:
  ENVIRONMENT: "production"
  APP_NAME: "Work Boots Console"
  DATABASE_URL: "postgresql+psycopg://postgres:postgres@postgres:5432/work_boots_console"
  GOOGLE_AUTH_ENABLED: "true"
  GOOGLE_OIDC_TOKENINFO_URL: "https://oauth2.googleapis.com/tokeninfo"
  GOOGLE_OIDC_TIMEOUT_SECONDS: "5"
  APP_SESSION_TTL_SECONDS: "3600"
  NEXT_PUBLIC_API_BASE_URL: "https://workboots.example.com"
