apiVersion: v1
kind: Service
metadata:
  name: work-boots-ui
spec:
  type: ClusterIP
  selector:
    app: work-boots-ui
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
