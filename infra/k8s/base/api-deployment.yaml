apiVersion: apps/v1
kind: Deployment
metadata:
  name: mbsrn-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mbsrn-api
  template:
    metadata:
      labels:
        app: mbsrn-api
    spec:
      containers:
        - name: api
          image: us-central1-docker.pkg.dev/PROJECT_ID/mbsrn/api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          command: ["python", "-m", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
          envFrom:
            - configMapRef:
                name: mbsrn-config
            - secretRef:
                name: mbsrn-secrets
          env:
            - name: REDIS_URL
              value: redis://mbsrn-redis:6379/0
            - name: SESSION_STATE_BACKEND
              value: redis
            - name: SESSION_STATE_ALLOW_INMEMORY_FALLBACK
              value: "false"
            - name: AI_PROVIDER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: mbsrn-ai-provider
                  key: AI_PROVIDER_API_KEY
                  optional: true
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 15
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "750m"
              memory: "1Gi"
