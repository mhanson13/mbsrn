apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  # Placeholder/example certificate resource name.
  name: mbsrn-managed-cert
spec:
  # Overlays can patch domains per environment.
  domains:
    - app.mbsrn.com
    - www.mbsrn.com
