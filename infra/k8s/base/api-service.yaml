apiVersion: v1
kind: Service
metadata:
  name: work-boots-api
spec:
  type: ClusterIP
  selector:
    app: work-boots-api
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
